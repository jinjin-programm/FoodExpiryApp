---
phase: 04-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - app/src/main/jni/CMakeLists.txt
  - app/src/main/jni/llamajni.cpp
  - app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlamaBridge.kt
  - app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlmVisionService.kt
  - app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlmScanFragment.kt
  - app/build.gradle.kts
  - app/src/main/jniLibs/arm64-v8a/libllama.so
  - app/src/main/jniLibs/arm64-v8a/libggml.so
  - app/src/main/jniLibs/arm64-v8a/libggml-base.so
  - app/src/main/jniLibs/arm64-v8a/libggml-cpu.so
  - app/src/main/jniLibs/arm64-v8a/libmtmd.so
  - app/src/main/jniLibs/arm64-v8a/libomp.so
autonomous: true
requirements:
  - MNN-02
user_setup: []

must_haves:
  truths:
    - "No llama.cpp native .so files exist in jniLibs"
    - "No llama.cpp JNI code exists (CMakeLists.txt, llamajni.cpp)"
    - "No llama.cpp Kotlin wrapper exists (LlamaBridge.kt)"
    - "No llama.cpp Java dependency exists in build.gradle.kts (com.llama4j:gguf)"
    - "Project builds successfully after llama.cpp removal"
  artifacts:
    - path: "app/src/main/jni/"
      provides: "JNI directory — must be empty or removed after llama.cpp cleanup"
      min_lines: 0
    - path: "app/build.gradle.kts"
      provides: "Build config without llama.cpp or GGUF references"
      contains: "NOT com.llama4j"
    - path: "app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/"
      provides: "LLM directory — must be empty or removed after llama.cpp cleanup"
  key_links:
    - from: "LlmScanFragment.kt"
      to: "LlamaBridge.kt"
      via: "import and usage"
      pattern: "LlamaBridge"
    - from: "build.gradle.kts"
      to: "llama.cpp"
      via: "externalNativeBuild cmake block"
      pattern: "CMakeLists.txt"
---

<objective>
Remove ALL llama.cpp artifacts from the project — native libraries, JNI code, Kotlin wrappers, and Gradle dependencies. This is an atomic, complete removal per PITFALL-2 (CMake libllm.so naming collision makes coexistence with MNN impossible).

Purpose: llama.cpp must be completely removed before MNN is added. Both ship a library named `libllm.so` — having both causes symbol conflicts, linker errors, or runtime crashes. Partial removal is worse than no removal.

Output: Zero llama.cpp references in the project. Build succeeds with native code removed. LLM scan UI files stubbed or removed gracefully.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/codebase/ARCHITECTURE.md

<interfaces>
From app/build.gradle.kts:
- Line 103-108: `externalNativeBuild { cmake { path = file("src/main/jni/CMakeLists.txt") } }`
- Line 196-199: TensorFlow Lite dependencies (kept for YOLO)
- Line 202: `implementation("com.llama4j:gguf:0.1.1")` — REMOVE

From app/src/main/jni/CMakeLists.txt:
- Links: libllama.so, libggml.so, libggml-base.so, libggml-cpu.so, libmtmd.so
- Builds: llamajni.cpp → libllama_jni.so

From LlamaBridge.kt:
- Singleton pattern with `System.loadLibrary("llama_jni")`
- Native methods: nativeLoadModel, nativeFreeModel, nativeGenerate, nativeLoadMmproj, nativeGenerateWithImage
- Used by: LlmScanFragment, LlmVisionService
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete llama.cpp native libraries and JNI code</name>
  <files>
    app/src/main/jniLibs/arm64-v8a/libllama.so
    app/src/main/jniLibs/arm64-v8a/libggml.so
    app/src/main/jniLibs/arm64-v8a/libggml-base.so
    app/src/main/jniLibs/arm64-v8a/libggml-cpu.so
    app/src/main/jniLibs/arm64-v8a/libmtmd.so
    app/src/main/jniLibs/arm64-v8a/libomp.so
    app/src/main/jni/CMakeLists.txt
    app/src/main/jni/llamajni.cpp
    app/app/src/main/jniLibs/arm64-v8a/libllama.so
    app/app/src/main/jniLibs/arm64-v8a/libggml.so
    app/app/src/main/jniLibs/arm64-v8a/libggml-base.so
    app/app/src/main/jniLibs/arm64-v8a/libggml-cpu.so
    app/app/src/main/jniLibs/arm64-v8a/libmtmd.so
    app/app/src/main/jni/CMakeLists.txt
    app/app/src/main/jni/llamajni.cpp
  </files>
  <read_first>
    - app/src/main/jni/CMakeLists.txt (verify llama.cpp references before deletion)
    - app/src/main/jni/llamajni.cpp (verify JNI bridge before deletion)
  </read_first>
  <action>
    1. Delete ALL llama.cpp native shared libraries from BOTH `app/src/main/jniLibs/` and `app/app/src/main/jniLibs/` (the project has a duplicate directory structure):
    - `libllama.so`
    - `libggml.so`
    - `libggml-base.so`
    - `libggml-cpu.so`
    - `libmtmd.so`
    - `libomp.so`

    Use: `rm app/src/main/jniLibs/arm64-v8a/libllama.so app/src/main/jniLibs/arm64-v8a/libggml.so app/src/main/jniLibs/arm64-v8a/libggml-base.so app/src/main/jniLibs/arm64-v8a/libggml-cpu.so app/src/main/jniLibs/arm64-v8a/libmtmd.so app/src/main/jniLibs/arm64-v8a/libomp.so`
    Also: `rm app/app/src/main/jniLibs/arm64-v8a/libllama.so app/app/src/main/jniLibs/arm64-v8a/libggml.so app/app/src/main/jniLibs/arm64-v8a/libggml-base.so app/app/src/main/jniLibs/arm64-v8a/libggml-cpu.so app/app/src/main/jniLibs/arm64-v8a/libmtmd.so`

    2. Delete the JNI CMakeLists.txt and llamajni.cpp from BOTH directories:
    `rm app/src/main/jni/CMakeLists.txt app/src/main/jni/llamajni.cpp`
    `rm app/app/src/main/jni/CMakeLists.txt app/app/src/main/jni/llamajni.cpp`

    3. After deletion, verify the `app/src/main/jni/` and `app/app/src/main/jni/` directories are empty or don't exist. If empty, remove them: `rmdir app/src/main/jni app/app/src/main/jni 2>/dev/null`

    4. Similarly check if `app/src/main/jniLibs/arm64-v8a/` and `app/app/src/main/jniLibs/arm64-v8a/` are now empty. If empty, remove them.

    5. Also delete any llama.cpp header files if they exist:
    `rm -rf app/src/main/jni/include app/app/src/main/jni/include 2>/dev/null`

    6. Check for and remove any llama.cpp model files in assets:
    `find app/src/main/assets -name "*.gguf" 2>/dev/null`
    If found, delete them. Also check for `llm/` subdirectory in assets.
  </action>
  <verify>
    <automated>find app -name "libllama*" -o -name "libggml*" -o -name "libmtmd*" -o -name "libomp*" -o -name "llamajni.cpp" -o -name "CMakeLists.txt" | grep -v ".gradle" | head -20</automated>
  </verify>
  <done>
    - Zero llama.cpp .so files remain in any jniLibs directory
    - No llamajni.cpp or CMakeLists.txt in jni/ directories
    - No llama.cpp header files remain
    - jni/ and jniLibs/ directories removed if empty
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove llama.cpp from Gradle and Kotlin code</name>
  <files>
    app/build.gradle.kts
    app/app/build.gradle.kts
    app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlamaBridge.kt
    app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlmVisionService.kt
    app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlmScanFragment.kt
  </files>
  <read_first>
    - app/build.gradle.kts (full file — modify dependencies and cmake block)
    - app/app/build.gradle.kts (check if duplicate exists)
    - app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlamaBridge.kt (file to delete)
    - app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlmVisionService.kt (file to delete)
    - app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlmScanFragment.kt (file to delete)
  </read_first>
  <action>
    **Step A: Modify build.gradle.kts**

    In `app/build.gradle.kts`, make these exact changes:

    1. REMOVE the `externalNativeBuild` block entirely (lines 103-108):
    ```
    // DELETE THIS ENTIRE BLOCK:
    externalNativeBuild {
        cmake {
            path = file("src/main/jni/CMakeLists.txt")
            version = "3.22.1"
        }
    }
    ```

    2. REMOVE the llama.cpp GGUF dependency (line 202):
    ```
    // DELETE THIS LINE:
    implementation("com.llama4j:gguf:0.1.1")
    ```

    3. REMOVE `mlModelBinding = true` from `buildFeatures` block (line 100) — this was for TFLite model binding which we may still need for YOLO, so ONLY remove it if TFLite is also being removed. For Phase 4, KEEP `mlModelBinding = true` because TFLite stays for YOLO.

    4. If `app/app/build.gradle.kts` exists (duplicate), apply the same changes.

    **Step B: Delete llama.cpp Kotlin wrapper files**

    Delete these files (and their duplicates in `app/app/` if they exist):
    - `app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlamaBridge.kt`
    - `app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlmVisionService.kt`
    - `app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm/LlmScanFragment.kt`

    After deletion, check if the `llm/` package directory is empty. If so, remove it:
    `rmdir app/src/main/java/com/example/foodexpiryapp/presentation/ui/llm 2>/dev/null`

    **Step C: Find and fix all references to deleted llama.cpp code**

    Search the entire codebase for references to the deleted classes:
    ```
    grep -rn "LlamaBridge" app/src/main/java/
    grep -rn "LlmVisionService" app/src/main/java/
    grep -rn "LlmScanFragment" app/src/main/java/
    grep -rn "import.*ui.llm" app/src/main/java/
    grep -rn "llama_jni" app/src/main/
    ```

    For each file that references the deleted code:
    - If it's a navigation graph XML referencing LlmScanFragment, comment out or remove that destination
    - If it's a Fragment or Activity importing/using LlamaBridge, remove those imports and usages. The feature is being rebuilt with MNN in Phase 5, so stub the functionality: replace LLM calls with a TODO comment
    - If it's a DI module providing LlamaBridge, remove that provider

    **Step D: Search for GGUF references**
    ```
    grep -rn "gguf\|GGUF\|llama4j" app/src/main/java/
    ```
    Remove any remaining GGUF/llama4j references.

    **Step E: Verify the project compiles**
    After all changes, attempt a Gradle sync or build:
    ```
    ./gradlew assembleDebug 2>&1 | tail -30
    ```
    If there are compilation errors from remaining references to deleted code, fix them. The goal is a clean build — even if LLM scanning features are temporarily non-functional (they'll be rebuilt with MNN in Phase 5).
  </action>
  <verify>
    <automated>grep -rn "LlamaBridge\|llama4j\|gguf\|llama_jni\|externalNativeBuild" app/build.gradle.kts app/src/main/java/ 2>/dev/null | grep -v ".gradle/" | head -20</automated>
  </verify>
  <done>
    - build.gradle.kts has NO `externalNativeBuild` cmake block
    - build.gradle.kts has NO `com.llama4j:gguf` dependency
    - LlamaBridge.kt, LlmVisionService.kt, LlmScanFragment.kt deleted
    - Zero references to LlamaBridge, LlmVisionService, LlmScanFragment in remaining code
    - Zero references to gguf, llama4j, llama_jni in remaining code
    - `./gradlew assembleDebug` succeeds (build passes)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This task only removes code and files. No new input paths introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | D | build.gradle.kts | accept | Only removing dependencies, no secrets added |
| T-04-04 | I | Navigation graph | mitigate | Comment out (don't delete) navigation destinations referencing removed fragments — prevents broken nav links at runtime |
</threat_model>

<verification>
1. `find app -name "libllama*" -o -name "libggml*" -o -name "libmtmd*" -o -name "libomp.so" | wc -l` returns 0
2. `grep -c "LlamaBridge\|llama4j\|externalNativeBuild" app/build.gradle.kts` returns 0
3. `grep -rn "LlamaBridge\|LlmVisionService\|LlmScanFragment" app/src/main/java/ | wc -l` returns 0
4. `./gradlew assembleDebug` exits with code 0
</verification>

<success_criteria>
- All 6 llama.cpp native .so files deleted from both `app/` and `app/app/` directories
- CMakeLists.txt and llamajni.cpp deleted from both jni/ directories
- LlamaBridge.kt, LlmVisionService.kt, LlmScanFragment.kt deleted
- build.gradle.kts cleaned: no cmake block, no gguf dependency
- All references to deleted code removed from remaining source files
- Project builds successfully with `./gradlew assembleDebug`
</success_criteria>

<output>
After completion, create `.planning/phases/04-foundation/04-02-SUMMARY.md`
</output>
