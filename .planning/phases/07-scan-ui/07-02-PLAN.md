---
phase: 07-scan-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/layout/fragment_vision_scan.xml
  - app/src/main/res/layout/fragment_scan.xml
  - app/src/main/res/layout/fragment_yolo_scan.xml
  - app/src/main/res/drawable/shape_fab_back.xml
autonomous: true
requirements: [UI-02, UI-07, UI-08]

must_haves:
  truths:
    - "All three scan screens have full-bleed camera with no visible frame/bounding box guide"
    - "All three scan screens have a floating back arrow button in top-left corner"
    - "DetectionOverlayView bounding boxes still render on YOLO scan during detection"
    - "All three scan modes use the same bottom-center capture button style"
  artifacts:
    - path: "app/src/main/res/layout/fragment_vision_scan.xml"
      provides: "Vision scan with no AppBarLayout, no simulated_box, floating back"
    - path: "app/src/main/res/layout/fragment_scan.xml"
      provides: "Barcode scan with no scan_box, no Flash/Close FABs, floating back, capture button"
    - path: "app/src/main/res/layout/fragment_yolo_scan.xml"
      provides: "YOLO scan with no frame_card, no top_controls, floating back"
    - path: "app/src/main/res/drawable/shape_fab_back.xml"
      provides: "Semi-transparent circular background for floating back arrow"
  key_links:
    - from: "fragment_yolo_scan.xml"
      to: "DetectionOverlayView"
      via: "view binding"
      pattern: "DetectionOverlayView"
---

<objective>
Redesign all three scan layouts for a clean, full-bleed camera experience: remove static framing guides, add floating back arrows, and unify capture button style across all modes.

Purpose: Per D-04 (floating back arrow), D-05 (remove frames), D-06 (all modes), D-07 (keep DetectionOverlayView), D-16 (consistent capture button). Implements UI-02 (horizontal/full-bleed camera), UI-07 (back button replaces close), UI-08 (consistent capture button).

Output: Three redesigned scan layouts with floating back buttons, no static frames, and unified capture button style.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scan-ui/07-CONTEXT.md

<!-- Wave 1 parallel — no dependency on Plan 01 -->

<interfaces>
<!-- Key types the executor needs from existing code -->

From fragment_yolo_scan.xml — existing capture button pattern to replicate:
```xml
<!-- Main Shutter (REUSE THIS EXACT PATTERN) -->
<FrameLayout
    android:id="@+id/btn_shutter_container"
    android:layout_width="96dp"
    android:layout_height="96dp"
    android:background="@drawable/shape_circle_shutter_ring">
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_capture"
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:backgroundTint="@color/white"
        app:cornerRadius="40dp"
        app:elevation="8dp" />
</FrameLayout>
```

From DetectionOverlayView (YOLO scan — KEEP this):
```xml
<com.example.foodexpiryapp.presentation.ui.yolo.DetectionOverlayView
    android:id="@+id/detectionOverlay"
    android:layout_width="match_parent"
    android:layout_height="0dp" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create floating back arrow drawable and redesign all three scan layouts</name>
  <files>app/src/main/res/drawable/shape_fab_back.xml, app/src/main/res/layout/fragment_vision_scan.xml, app/src/main/res/layout/fragment_scan.xml, app/src/main/res/layout/fragment_yolo_scan.xml</files>
  <action>
## Step 1: Create floating back arrow drawable

Create `app/src/main/res/drawable/shape_fab_back.xml` — a semi-transparent circle (for the floating back button background). Use a `<shape android:shape="oval">` with solid color `#33FFFFFF` (semi-transparent white, matching existing YOLO close button style from the current layout).

## Step 2: Redesign fragment_vision_scan.xml

Per D-04, D-05:
- **REMOVE** the entire `AppBarLayout` block (lines 10-48) including the toolbar, btn_close, and viewStatusIndicator
- **REMOVE** the `simulated_box` MaterialCardView (lines 67-81) — this is the static frame guide
- **REMOVE** the `tv_status` text above the box (lines 84-98) — no more status pill above frame
- **REMOVE** the tint overlay View (lines 59-64) with `#261B1C1A` background — full-bleed means no tint
- **KEEP** the PreviewView but change it to fill the entire parent: `layout_constraintTop_toTopOf="parent"` and `layout_constraintBottom_toBottomOf="parent"` (remove constraint to appBar)
- **KEEP** the resultCard, capture button section (btn_capture), and all hidden elements (progressBar, tv_progress_detail, etc.)
- **ADD** a floating back arrow `ImageButton` in the top-left:
  ```xml
  <ImageButton
      android:id="@+id/btn_back"
      android:layout_width="48dp"
      android:layout_height="48dp"
      android:layout_marginStart="16dp"
      android:layout_marginTop="24dp"
      android:background="@drawable/shape_fab_back"
      android:contentDescription="Back"
      android:src="@android:drawable/ic_menu_revert"
      app:tint="@color/white"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintTop_toTopOf="parent" />
  ```
- The existing `btn_capture` in the FrameLayout at the bottom already works — keep it as-is. It already has the circular design with `shape_circle_outline`. Per D-16, this style is close enough to the YOLO shutter style. No change needed.

## Step 3: Redesign fragment_scan.xml

Per D-04, D-05, D-13:
- **REMOVE** the `scan_box` MaterialCardView (lines 30-57) — the square frame with scanning line
- **REMOVE** the `tv_instruction` "Align barcode within the frame" (lines 60-71)
- **REMOVE** the `tv_instruction_detail` "Scanner will lock in automatically" (lines 73-83)
- **REMOVE** the bottom Flash/Close FAB buttons (lines 101-164) — both `btn_flash` and `btn_close` LinearLayout containers
- **KEEP** the PreviewView (`viewFinder`) — already full-bleed filling the parent. Good.
- **REMOVE** the tint overlay View (lines 20-27) with `#260F120F`
- **KEEP** the `card_last_scanned` (will be repurposed for barcode result card in Plan 04)
- **KEEP** the `btn_manual_entry` button
- **ADD** a floating back arrow `ImageButton` (same style as above, id=`btn_back`):
  ```xml
  <ImageButton
      android:id="@+id/btn_back"
      android:layout_width="48dp"
      android:layout_height="48dp"
      android:layout_marginStart="16dp"
      android:layout_marginTop="24dp"
      android:background="@drawable/shape_fab_back"
      android:contentDescription="Back"
      android:src="@android:drawable/ic_menu_revert"
      app:tint="@color/white"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintTop_toTopOf="parent" />
  ```
- **ADD** a capture button at bottom-center (matching YOLO shutter style):
  ```xml
  <FrameLayout
      android:id="@+id/btn_shutter_container"
      android:layout_width="96dp"
      android:layout_height="96dp"
      android:layout_marginBottom="32dp"
      android:background="@drawable/shape_circle_shutter_ring"
      app:layout_constraintBottom_toBottomOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      app:layout_constraintStart_toStartOf="parent">

      <com.google.android.material.button.MaterialButton
          android:id="@+id/btn_capture"
          android:layout_width="80dp"
          android:layout_height="80dp"
          android:layout_gravity="center"
          android:insetLeft="0dp"
          android:insetTop="0dp"
          android:insetRight="0dp"
          android:insetBottom="0dp"
          app:backgroundTint="@color/white"
          app:cornerRadius="40dp"
          app:elevation="8dp" />
  </FrameLayout>
  ```

## Step 4: Redesign fragment_yolo_scan.xml

Per D-04, D-05:
- **REMOVE** the `frame_card` MaterialCardView (lines 87-111) — the 3:4 static frame with scanning line
- **REMOVE** the `top_controls` LinearLayout (lines 36-85) — the "Single Item" / "Multi-Scan" mode pills and the `btn_close` button
- **REMOVE** the `tv_instruction` text (lines 209-225) — "Align items within the frame"
- **REMOVE** the tint overlay View (lines 27-33) with `#261B1C1A` background
- **KEEP** the PreviewView (`viewFinder`) — already full-bleed, good
- **KEEP** `DetectionOverlayView` (lines 18-24) — per D-07, this stays for drawing bounding boxes during detection
- **KEEP** the `bottom_controls` section with `btn_shutter_container`/`btn_capture`, `gallery_container`, and `btn_switch_camera`
- **KEEP** the `progressOverlay` — stays for staged progress
- **ADD** a floating back arrow `ImageButton` (id=`btn_back`, same style):
  ```xml
  <ImageButton
      android:id="@+id/btn_back"
      android:layout_width="48dp"
      android:layout_height="48dp"
      android:layout_marginStart="16dp"
      android:layout_marginTop="24dp"
      android:background="@drawable/shape_fab_back"
      android:contentDescription="Back"
      android:src="@android:drawable/ic_menu_revert"
      app:tint="@color/white"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintTop_toTopOf="parent" />
  ```
- The DetectionOverlayView must have constraints matching the PreviewView (fill parent top to bottom)

## Important constraints across all three layouts:
- PreviewView must be `match_parent` / `0dp` constrained to parent top and bottom (full-bleed)
- DetectionOverlayView must be `match_parent` / `0dp` constrained same as PreviewView
- The floating back arrow uses `@android:drawable/ic_menu_revert` (system back arrow icon)
- The shape_fab_back.xml provides the semi-transparent circular background
- Do NOT touch any Kotlin files in this task — only layouts and the new drawable
  </action>
  <verify>
    <automated>./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>
    - shape_fab_back.xml drawable created with semi-transparent circle
    - fragment_vision_scan.xml: AppBarLayout and simulated_box removed, floating btn_back added, PreviewView full-bleed
    - fragment_scan.xml: scan_box, Flash/Close FABs, instructions removed, floating btn_back and capture button added, PreviewView full-bleed
    - fragment_yolo_scan.xml: frame_card and top_controls removed, floating btn_back added, DetectionOverlayView kept, PreviewView full-bleed
    - All three layouts build successfully
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layout XML → Camera | Full-bleed camera preview — no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | E | Layout XML | accept | UI-only changes, no data boundary |
</threat_model>

<verification>
1. `./gradlew assembleDebug` passes
2. No `AppBarLayout` in fragment_vision_scan.xml
3. No `scan_box` in fragment_scan.xml
4. No `frame_card` or `top_controls` in fragment_yolo_scan.xml
5. `shape_fab_back.xml` exists
6. `DetectionOverlayView` still present in fragment_yolo_scan.xml
7. All three layouts have `btn_back` ImageButton
</verification>

<success_criteria>
- All three scan layouts redesigned with full-bleed camera
- Floating back arrows on all scan screens
- No static frame guides anywhere
- DetectionOverlayView preserved for YOLO bounding boxes
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-scan-ui/07-02-SUMMARY.md`
</output>
