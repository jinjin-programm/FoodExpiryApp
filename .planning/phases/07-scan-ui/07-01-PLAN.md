---
phase: 07-scan-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/themes.xml
  - app/src/main/java/com/example/foodexpiryapp/MainActivity.kt
  - app/src/main/res/layout/activity_main.xml
autonomous: true
requirements: [UI-01, UI-07]

must_haves:
  truths:
    - "No 'FoodExpiryApp' title bar appears on any main tab page — only bottom nav is visible"
    - "Detail screens that need navigation have their own Toolbar with back button"
    - "App launches successfully with NoActionBar theme and no crashes"
  artifacts:
    - path: "app/src/main/res/values/themes.xml"
      provides: "NoActionBar theme definition"
      contains: "NoActionBar"
    - path: "app/src/main/java/com/example/foodexpiryapp/MainActivity.kt"
      provides: "MainActivity with NoActionBar compatibility"
  key_links:
    - from: "MainActivity.kt"
      to: "themes.xml"
      via: "theme reference in AndroidManifest"
      pattern: "Theme\\.FoodExpiryAPP"
---

<objective>
Switch the app theme from DarkActionBar to NoActionBar globally, remove the default action bar from all main tabs, and ensure detail screens manage their own toolbars.

Purpose: Per D-01, switch theme globally so every fragment manages its own top bar. Per D-02, main tabs get NO toolbar at all — just bottom nav. Per D-03, detail screens that need navigation add their own Toolbar. Implements UI-01 (title bar removed) and UI-07 (close button replaced with back button on scan screens — toolbar infrastructure).

Output: Modified themes.xml with NoActionBar, updated MainActivity, verified build.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scan-ui/07-CONTEXT.md

@app/src/main/res/values/themes.xml
@app/src/main/java/com/example/foodexpiryapp/MainActivity.kt
@app/src/main/res/layout/activity_main.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch theme to NoActionBar and update MainActivity</name>
  <files>app/src/main/res/values/themes.xml, app/src/main/java/com/example/foodexpiryapp/MainActivity.kt, app/src/main/res/layout/activity_main.xml</files>
  <action>
Per D-01: Change theme parent from `Theme.MaterialComponents.DayNight.DarkActionBar` to `Theme.MaterialComponents.DayNight.NoActionBar` in themes.xml.

Per D-02: Main tabs (Inventory, Shopping, Recipes, Planner, Profile) get NO toolbar at all — just bottom nav. The current activity_main.xml has no toolbar, so this is already correct structurally. The NoActionBar theme will remove the default ActionBar that Android auto-creates.

In MainActivity.kt:
- Remove any `setSupportActionBar()` calls if they exist (there are none currently — good)
- The activity uses ViewPager2 + BottomNavigationView, no toolbar — NoActionBar is a clean fit
- Remove the `android:statusBarColor` line from themes.xml if it causes visual issues, or keep it for consistent status bar coloring. Keep `colorPrimaryVariant` as status bar color since it provides visual continuity.

Verify that the AndroidManifest.xml references `@style/Theme.FoodExpiryAPP` (it should by default). No changes needed to manifest.

Per D-03: Detail screens already manage their own toolbars or use Navigation Component's back stack:
- ConfirmationFragment already has its own toolbar (verified in CONTEXT.md)
- RecipeDetailFragment uses Navigation Component back
- ChatFragment uses Navigation Component back
- Scan fragments will get floating back arrows (Plan 02)
- No additional toolbar setup needed for detail screens

DO NOT add any new toolbar to main tabs. DO NOT add a Toolbar view to activity_main.xml. The point is that main tabs have NO toolbar at all.
  </action>
  <verify>
    <automated>./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>
    - themes.xml uses NoActionBar parent
    - App builds successfully with ./gradlew assembleDebug
    - No crashes from missing action bar (MainActivity never calls setSupportActionBar)
    - Main tabs show only bottom nav, no title bar
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme → System | NoActionBar theme affects system chrome — no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | E | themes.xml | accept | UI-only change, no data exposure risk |
</threat_model>

<verification>
1. `./gradlew assembleDebug` passes
2. themes.xml contains `NoActionBar`
3. MainActivity.kt has no `setSupportActionBar` calls
</verification>

<success_criteria>
- Theme switched to NoActionBar without breaking any existing screen
- Main tab pages have no title bar
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-scan-ui/07-01-SUMMARY.md`
</output>
