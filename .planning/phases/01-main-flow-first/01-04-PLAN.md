---
phase: 01-main-flow-first
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified: 
  - "app/src/main/res/layout/dialog_add_food.xml"
  - "app/src/main/java/com/example/foodexpiryapp/ui/inventory/AddFoodBottomSheet.kt"
autonomous: true
requirements: ["INVN-02"]

must_haves:
  truths:
    - "User can review pre-filled OCR data before saving an item"
    - "User can edit pre-filled data in a confirmation modal"
  artifacts:
    - path: "app/src/main/res/layout/dialog_add_food.xml"
      provides: "OCR confirmation form fields"
    - path: "app/src/main/java/com/example/foodexpiryapp/ui/inventory/AddFoodBottomSheet.kt"
      provides: "Data binding and saving logic for the scanned item"
  key_links:
    - from: "app/src/main/java/com/example/foodexpiryapp/ui/inventory/AddFoodBottomSheet.kt"
      to: "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryViewModel.kt"
      via: "Saving the verified item to the unified data model"
---

<objective>
Implement the "Photo Scan" OCR Confirmation Flow.

Purpose: To ensure users can verify and correct OCR-extracted data before it is saved to the inventory.
Output: A bottom sheet or dialog populated with pre-filled OCR data, allowing edits and final confirmation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-main-flow-first/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OCR Confirmation UI</name>
  <files>app/src/main/res/layout/dialog_add_food.xml, app/src/main/res/layout/fragment_scan.xml</files>
  <action>
    Update `dialog_add_food.xml` to include editable fields for Name, Date, Category, and Quantity, following the 8dp spacing and typography from `01-UI-SPEC.md`. Add a primary CTA Button "Save to Inventory".
    In `fragment_scan.xml`, ensure there is a loading state layout ("Analyzing image...") with a circular progress indicator that can be toggled while OCR is running.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>UI layouts have the correct fields and loading states defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Pre-fill and Save Logic</name>
  <files>app/src/main/java/com/example/foodexpiryapp/ui/inventory/AddFoodBottomSheet.kt, app/src/main/java/com/example/foodexpiryapp/ui/scan/ScanFragment.kt</files>
  <action>
    If `AddFoodBottomSheet.kt` exists, update it to accept pre-filled arguments (Name, Expiry Date, Category, Confidence level). If missing, create it.
    When the user confirms the details, map the input to a `FoodItem` with `ScanSource` set correctly (e.g., OCR) and save it via the ViewModel.
    In `ScanFragment.kt` (or the relevant scanner fragment), upon receiving a mock/actual OCR result, show the bottom sheet pre-filled with the data.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Scanned data is passed to the bottom sheet, editable by the user, and saved to the data layer.</done>
</task>

</tasks>

<verification>
- Scanner triggers the confirmation bottom sheet.
- Bottom sheet is populated with extracted fields.
- Saving the sheet adds the item to the database.
</verification>

<success_criteria>
- App compiles successfully.
- Follows the "Photo Scan" flow in 01-UI-SPEC.md.
</success_criteria>

<output>
After completion, create `.planning/phases/01-main-flow-first/01-04-SUMMARY.md`
</output>