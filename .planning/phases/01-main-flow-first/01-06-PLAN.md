---
phase: 01-main-flow-first
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/src/main/java/com/example/foodexpiryapp/presentation/ui/inventory/AddFoodBottomSheet.kt"
  - "app/src/main/java/com/example/foodexpiryapp/presentation/ui/scan/ScanFragment.kt"
autonomous: true
gap_closure: true
requirements: [INVN-02]
must_haves:
  truths:
    - "User can review pre-filled OCR data before saving an item"
    - "User can edit pre-filled data in a confirmation modal"
  artifacts:
    - path: "app/src/main/java/com/example/foodexpiryapp/presentation/ui/inventory/AddFoodBottomSheet.kt"
      provides: "OCR Data review form"
    - path: "app/src/main/java/com/example/foodexpiryapp/presentation/ui/scan/ScanFragment.kt"
      provides: "Launcher for AddFoodBottomSheet"
  key_links:
    - from: "app/src/main/java/com/example/foodexpiryapp/presentation/ui/inventory/AddFoodBottomSheet.kt"
      to: "app/src/main/res/layout/dialog_add_food.xml"
      via: "Data binding or view inflation"
---

<objective>
Implement the missing AddFoodBottomSheet for OCR confirmation.

Purpose: Address the VERIFICATION gaps where Plan 04 skipped the creation of `AddFoodBottomSheet.kt`.
Output: A working `AddFoodBottomSheet` that displays scanned data and allows editing before saving.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AddFoodBottomSheet</name>
  <files>app/src/main/java/com/example/foodexpiryapp/presentation/ui/inventory/AddFoodBottomSheet.kt</files>
  <action>
    Create the missing class `AddFoodBottomSheet` in the `presentation/ui/inventory` package (NOT `ui/inventory/`).
    
    It should extend `BottomSheetDialogFragment`.
    Inflate `R.layout.dialog_add_food`.
    Pre-fill the editable fields (name, expiry date, etc.) using arguments passed in.
    Wire the save button to insert a `FoodItem` via `InventoryViewModel` or a shared `PlannerViewModel` / interface callback.
    
    Gap reason: The file was not created by Plan 04.
  </action>
  <verify>
    <automated>test -f app/src/main/java/com/example/foodexpiryapp/presentation/ui/inventory/AddFoodBottomSheet.kt</automated>
  </verify>
  <done>The bottom sheet dialog class exists and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Scanner to Bottom Sheet</name>
  <files>app/src/main/java/com/example/foodexpiryapp/presentation/ui/scan/ScanFragment.kt</files>
  <action>
    Modify `ScanFragment` (or whichever scanner fragment handles OCR results) to instantiate and show `AddFoodBottomSheet` when OCR scanning completes or the user decides to save.
    
    Pass the scanned metadata (name, expiry date, scan source, confidence) to the Bottom Sheet either through navigation args or fragment bundle.
    
    Gap reason: The scanner flow was disconnected because the bottom sheet was missing.
  </action>
  <verify>
    <automated>grep -q "AddFoodBottomSheet" app/src/main/java/com/example/foodexpiryapp/presentation/ui/scan/ScanFragment.kt</automated>
  </verify>
  <done>ScanFragment instantiates AddFoodBottomSheet when appropriate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| OCR -> UI | Untrusted OCR text mapped into the confirmation form |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Spoofing | AddFoodBottomSheet | accept | OCR text is only displayed and optionally edited before being persisted via Room (which prevents SQL injection). It is not rendered as HTML. |
</threat_model>

<verification>
App compiles successfully, the bottom sheet launches when triggered, and user edits are respected before saving.
</verification>

<success_criteria>
- AddFoodBottomSheet.kt exists and is integrated.
- Users can review and edit OCR results.
</success_criteria>

<output>
After completion, create `.planning/phases/01-main-flow-first/01-06-SUMMARY.md`
</output>
