---
phase: 01-main-flow-first
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: 
  - "app/src/main/res/layout/fragment_inventory.xml"
  - "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt"
autonomous: true
requirements: ["INVN-01", "INVN-05"]

must_haves:
  truths:
    - "User can see a clear 'Get Started' block when inventory is empty"
    - "User can tap a quick actions dropdown/FAB menu with options for Photo scan, Manual entry, View expiring, View analysis"
  artifacts:
    - path: "app/src/main/res/layout/fragment_inventory.xml"
      provides: "Empty state view and Quick Actions Speed Dial/Menu"
    - path: "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt"
      provides: "Visibility toggling for empty state and FAB click listeners"
  key_links:
    - from: "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt"
      to: "app/src/main/res/layout/fragment_inventory.xml"
      via: "View binding visibility changes based on data list size"
---

<objective>
Implement the "Get Started" empty state and the Quick Actions dropdown/FAB for the Inventory screen.

Purpose: To guide new users immediately to their first action (scanning) and provide easy access to primary inventory functions.
Output: Working empty state UI that appears only when no items exist, and a functional quick actions menu.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-main-flow-first/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Empty State UI</name>
  <files>app/src/main/res/layout/fragment_inventory.xml, app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt</files>
  <action>
    Add a LinearLayout container in `fragment_inventory.xml` for the empty state. It should be centered, contain an illustration/icon (e.g., ic_inventory), a title ("Get Started", 22sp Medium), body text ("Your inventory is empty. Start by scanning a food item..."), and a primary CTA Button ("Scan Item", primary color).
    In `InventoryFragment.kt`, observe the inventory list data; if empty, set the empty state container visibility to VISIBLE and hide the RecyclerView.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Empty state layout is present in XML and toggled correctly in Fragment based on data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Quick Actions FAB Menu</name>
  <files>app/src/main/res/layout/fragment_inventory.xml, app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt, app/src/main/res/menu/inventory_quick_actions.xml</files>
  <action>
    Create a menu resource for the quick actions (Photo scan, Manual entry, View expiring, View analysis).
    In `fragment_inventory.xml`, ensure there is a main FAB.
    In `InventoryFragment.kt`, attach a PopupMenu to the FAB using the new menu, or implement an ExtendedFloatingActionButton / Speed Dial approach. Wire click listeners to placeholders (Toasts are fine for now if destination doesn't exist).
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Quick actions menu displays when FAB is clicked with the 4 required options.</done>
</task>

</tasks>

<verification>
- Empty state shows when list is empty.
- Quick actions menu contains 4 specific options.
</verification>

<success_criteria>
- App compiles successfully.
- XML layout follows 01-UI-SPEC.md typography and spacing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-main-flow-first/01-02-SUMMARY.md`
</output>