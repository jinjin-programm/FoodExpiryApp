---
phase: 01-main-flow-first
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified: 
  - "app/src/main/res/layout/fragment_inventory.xml"
  - "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt"
  - "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryViewModel.kt"
autonomous: true
requirements: ["INVN-03", "INVN-04"]

must_haves:
  truths:
    - "User can see an 'Expiring Soon' section sorted by risk level"
    - "User can search items specific to the current active tab"
  artifacts:
    - path: "app/src/main/res/layout/fragment_inventory.xml"
      provides: "Expiring soon list/cards and search view"
    - path: "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryViewModel.kt"
      provides: "Sorting logic by risk level and tab-aware filtering"
  key_links:
    - from: "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryViewModel.kt"
      to: "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt"
      via: "StateFlows for search text, current tab, and filtered list"
---

<objective>
Implement the "Expiring Soon" risk-sorted cards section and Tab-specific search functionality.

Purpose: To give users clear visibility into their immediate food risks and allow them to quickly locate items within categories.
Output: Functional "Expiring Soon" header and sorted list, plus a functional search bar that respects the currently selected tab.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-main-flow-first/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Expiring Soon Section UI</name>
  <files>app/src/main/res/layout/fragment_inventory.xml, app/src/main/res/layout/item_food_card.xml</files>
  <action>
    Update `fragment_inventory.xml` to include an "Expiring Soon" header (22sp Medium) and a horizontal RecyclerView below the search bar but above the main tab list.
    Update `item_food_card.xml` to display semantic risk badge colors (Expired=error, 1 day=orange, 2-3 days=warning, 4-7 days=primary) as defined in 01-UI-SPEC.md.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>UI for Expiring Soon exists with semantic color references.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Sorting and Tab-Aware Search</name>
  <files>app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryViewModel.kt, app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt</files>
  <action>
    In `InventoryViewModel.kt`, create a `Flow` or `StateFlow` that emits only items expiring in <= 7 days, sorted by risk level (expired first, then 1 day, 2-3, 4-7).
    Update the main inventory filter logic: when filtering by search query, also apply the currently selected tab's category. Only items matching the query AND the tab category should be shown in the main list.
    In `InventoryFragment.kt`, bind the search bar text changes and tab selection changes to the ViewModel. Update the UI to show "No items found..." when the filtered list is empty.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>ViewModel correctly filters items by tab and query, and correctly sorts the 'Expiring Soon' list.</done>
</task>

</tasks>

<verification>
- Expiring items are sorted by highest risk first.
- Typing in search only filters items in the current tab.
</verification>

<success_criteria>
- App compiles and tests pass.
- Risk badges reflect correct color logic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-main-flow-first/01-03-SUMMARY.md`
</output>