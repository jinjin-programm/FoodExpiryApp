---
phase: 01-main-flow-first
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/src/main/res/layout/fragment_inventory.xml"
  - "app/src/main/java/com/example/foodexpiryapp/presentation/ui/inventory/InventoryFragment.kt"
  - "app/src/main/java/com/example/foodexpiryapp/presentation/viewmodel/InventoryViewModel.kt"
  - "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt"
  - "app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryViewModel.kt"
autonomous: true
gap_closure: true
requirements: [INVN-01, INVN-05, INVN-03, INVN-04]
must_haves:
  truths:
    - "User can see a clear 'Get Started' block when inventory is empty"
    - "User can tap a quick actions dropdown/FAB menu with options for Photo scan, Manual entry, View expiring, View analysis"
    - "User can see an 'Expiring Soon' section sorted by risk level"
    - "User can search items specific to the current active tab"
  artifacts:
    - path: "app/src/main/res/layout/fragment_inventory.xml"
      provides: "Empty state, FAB, Search bar, and Expiring Soon UI"
    - path: "app/src/main/java/com/example/foodexpiryapp/presentation/ui/inventory/InventoryFragment.kt"
      provides: "UI wiring for empty state, FAB, and search"
    - path: "app/src/main/java/com/example/foodexpiryapp/presentation/viewmodel/InventoryViewModel.kt"
      provides: "Sorting and filtering logic"
  key_links:
    - from: "app/src/main/java/com/example/foodexpiryapp/presentation/ui/inventory/InventoryFragment.kt"
      to: "app/src/main/res/layout/fragment_inventory.xml"
      via: "View Binding matching exactly the restored IDs"
---

<objective>
Fix the Inventory UI architecture by removing conflicting stub files, restoring the broken XML layout, and properly implementing the Empty State, Quick Actions FAB, Expiring Soon list, and Tab-Aware Search bar.

Purpose: Address the VERIFICATION gaps where Plan 03 aggressively stubbed out the Inventory flow and broke compilation.
Output: A compiling and fully functional Inventory Fragment UI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Conflicting Stubs</name>
  <files>
    app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt,
    app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryViewModel.kt
  </files>
  <action>
    Delete the stub files generated by Plan 03 in the incorrect directory (`ui/`).
    
    Gap reason: Conflicting architecture paths caused by generating empty stubs in `ui/` while the real app uses `presentation/ui/`.
  </action>
  <verify>
    <automated>test ! -f app/src/main/java/com/example/foodexpiryapp/ui/inventory/InventoryFragment.kt</automated>
  </verify>
  <done>The conflicting stub files are deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Restore and Complete fragment_inventory.xml</name>
  <files>app/src/main/res/layout/fragment_inventory.xml</files>
  <action>
    Restore the full XML layout for the inventory. Replace the single-line placeholder comment with a real layout.
    
    Must include:
    - Empty state view (`layoutEmptyState`) from Plan 02.
    - Quick actions FAB (`fabQuickActions`) from Plan 02.
    - Search Bar (e.g. `searchView` or `etSearch`) for Tab-Aware Search from Plan 03.
    - "Expiring Soon" RecyclerView (`rvExpiringSoon`) from Plan 03.
    - Standard inventory lists.
    
    Gap reason: The file was overwritten with a stub, breaking all previous UI work.
  </action>
  <verify>
    <automated>grep "fabQuickActions" app/src/main/res/layout/fragment_inventory.xml</automated>
  </verify>
  <done>XML file contains all required component IDs and parses correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Presentation UI & ViewModel</name>
  <files>
    app/src/main/java/com/example/foodexpiryapp/presentation/ui/inventory/InventoryFragment.kt,
    app/src/main/java/com/example/foodexpiryapp/presentation/viewmodel/InventoryViewModel.kt
  </files>
  <action>
    Update the `presentation` layer files to connect to the newly restored XML IDs.
    
    - In `InventoryFragment`: Fix the compile errors. Re-bind the FAB to the Quick Actions popup menu. Bind the search input to update the ViewModel.
    - In `InventoryViewModel`: Implement tab-aware filtering and expiring soon sorting logic based on the `RiskLevel`.
    
    Gap reason: The fragment failed to compile because the layout IDs were deleted by the stub layout.
  </action>
  <verify>
    <automated>./gradlew app:assembleDebug</automated>
  </verify>
  <done>App compiles successfully with the fixed UI logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> ViewModel | User input from search |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | Search Bar | accept | Search input is local filtering only; no SQL injection risk if using Room correctly via parameter binding. |
</threat_model>

<verification>
App compiles and the fragment displays the empty state or expiring soon items appropriately.
</verification>

<success_criteria>
- Stubs are deleted.
- Inventory UI works again without compilation errors.
- Both empty state and search/expiring functionality exist.
</success_criteria>

<output>
After completion, create `.planning/phases/01-main-flow-first/01-05-SUMMARY.md`
</output>
