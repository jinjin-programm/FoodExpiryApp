---
phase: 08-yolo-hardening
plan: 02
type: execute
wave: 2
depends_on: ["08-08-PLAN"]
files_modified:
  - app/src/main/java/com/example/foodexpiryapp/di/InferenceModule.kt
  - app/src/main/java/com/example/foodexpiryapp/presentation/ui/vision/VisionScanFragment.kt
  - app/src/main/java/com/example/foodexpiryapp/presentation/ui/yolo/YoloScanFragment.kt
autonomous: true
requirements: [YOLO-01, YOLO-03]

must_haves:
  truths:
    - "InferenceModule provides OnnxYoloEngine as YoloDetector via Hilt"
    - "DetectionPipeline uses OnnxYoloEngine through YoloDetector interface"
    - "Debug overlay shows YOLOE detection bounding boxes after local detection"
    - "Logcat shows OnnxYoloEngine TAG with detection results (label, confidence, bbox)"
    - "Build compiles and OnnxYoloEngine is injected"
  artifacts:
    - path: "app/src/main/java/com/example/foodexpiryapp/di/InferenceModule.kt"
      provides: "Hilt DI binding for OnnxYoloEngine → YoloDetector"
      contains: "OnnxYoloEngine"
  key_links:
    - from: "InferenceModule.kt"
      to: "OnnxYoloEngine.kt"
      via: "Provides OnnxYoloEngine as YoloDetector"
      pattern: "provideYoloDetector.*OnnxYoloEngine"
    - from: "DetectionPipeline.kt"
      to: "OnnxYoloEngine.kt"
      via: "YoloDetector interface injection"
      pattern: "YoloDetector"
---

<objective>
Wire OnnxYoloEngine into the DI graph and add debug overlay for YOLOE detection verification.

Purpose: Per D-15 to D-18, developers need visual confirmation that YOLOE detections are correct before relying on them for LLM classification. The debug overlay draws bounding boxes on the camera preview after YOLO detection completes.

Output: OnnxYoloEngine injected via Hilt, debug overlay rendering detection boxes with labels and confidence scores.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-yolo-hardening/08-CONTEXT.md
@.planning/phases/08-yolo-hardening/08-08-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (OnnxYoloEngine) -->

From app/src/main/java/com/example/foodexpiryapp/inference/tflite/OnnxYoloEngine.kt (created in Plan 01):
```kotlin
@Singleton
class OnnxYoloEngine @Inject constructor(
    private val lifecycleManager: ModelLifecycleManager,
    @ApplicationContext private val context: Context
) : YoloDetector
```

From app/src/main/java/com/example/foodexpiryapp/di/InferenceModule.kt (current — needs update):
```kotlin
@Provides @Singleton
fun provideYoloDetector(engine: TFLiteYoloEngine): YoloDetector {
    return engine
}
```

From app/src/main/java/com/example/foodexpiryapp/presentation/ui/yolo/DetectionOverlayView.kt:
```kotlin
class DetectionOverlayView ... : View {
    fun setDetections(detections: List<DetectionResult>, imageWidth: Int, imageHeight: Int)
    fun clearDetections()
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update InferenceModule to provide OnnxYoloEngine</name>
  <files>app/src/main/java/com/example/foodexpiryapp/di/InferenceModule.kt</files>
  <read_first>
    - app/src/main/java/com/example/foodexpiryapp/di/InferenceModule.kt (current DI bindings)
    - app/src/main/java/com/example/foodexpiryapp/inference/tflite/OnnxYoloEngine.kt (new engine from Plan 01)
  </read_first>
  <action>
Per D-07: OnnxYoloEngine replaces TFLiteYoloEngine. Per D-09: YoloDetector interface unchanged.

In `InferenceModule.kt`:
1. Add import for `com.example.foodexpiryapp.inference.tflite.OnnxYoloEngine`
2. Change `provideYoloDetector()` from:
   ```kotlin
   fun provideYoloDetector(engine: TFLiteYoloEngine): YoloDetector = engine
   ```
   To:
   ```kotlin
   fun provideYoloDetector(engine: OnnxYoloEngine): YoloDetector = engine
   ```
3. Keep TFLiteYoloEngine import for now (other code may reference it), but the DI binding switches to OnnxYoloEngine
4. Keep all other bindings unchanged (Ollama, LM Studio, LlmInferenceRepository)
  </action>
  <acceptance_criteria>
    - `InferenceModule.kt` contains `import...OnnxYoloEngine`
    - `provideYoloDetector` parameter type is `OnnxYoloEngine` (not `TFLiteYoloEngine`)
    - All other bindings unchanged (OllamaVisionClient, LmStudioVisionClient, etc.)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>OnnxYoloEngine is the Hilt-provided YoloDetector implementation</done>
</task>

<task type="auto">
  <name>Task 2: Add debug overlay rendering for YOLOE detections</name>
  <files>
    - app/src/main/java/com/example/foodexpiryapp/presentation/ui/vision/VisionScanFragment.kt
    - app/src/main/java/com/example/foodexpiryapp/presentation/ui/yolo/YoloScanFragment.kt
  </files>
  <read_first>
    - app/src/main/java/com/example/foodexpiryapp/presentation/ui/vision/VisionScanFragment.kt (current multi-mode flow)
    - app/src/main/java/com/example/foodexpiryapp/presentation/ui/yolo/YoloScanFragment.kt (YOLO scan with overlay)
    - app/src/main/java/com/example/foodexpiryapp/presentation/ui/yolo/DetectionOverlayView.kt (overlay rendering)
    - app/src/main/java/com/example/foodexpiryapp/inference/pipeline/DetectionPipeline.kt (emits PipelineState.Detected)
  </read_first>
  <action>
Per D-15: Debug mode shows bounding boxes after YOLO detection. Per D-16: Overlay uses existing DetectionOverlayView. Per D-17: Debug controlled by BuildConfig.DEBUG. Per D-18: Logcat shows detection results.

In `YoloScanFragment.kt`:
1. After receiving `PipelineState.Detected(detections)`, if `BuildConfig.DEBUG`:
   - Call `detectionOverlayView.setDetections(detections, imageWidth, imageHeight)` to show bounding boxes
   - Add a `Handler(Looper.getMainLooper()).postDelayed({ ... }, 3000)` to clear overlay after 3 seconds (per D-16)
   - Log each detection: `Log.d("OnnxYoloEngine-Debug", "Detection: label=${det.label}, conf=${det.confidence}, bbox=${det.boundingBox}")`
2. The overlay already exists in the YoloScanFragment layout — just ensure it's being used in the Detected state handler

In `VisionScanFragment.kt` (multi-mode flow):
1. In the multi-mode detection flow, when receiving `PipelineState.Detected(detections)`:
   - If `BuildConfig.DEBUG`, log all detections with tag "OnnxYoloEngine-Debug"
   - The VisionScanFragment does NOT have DetectionOverlayView (it's only in YoloScanFragment), so just log here

The overlay pause gives developers 3 seconds to see the bounding boxes before LLM classification proceeds. In production (BuildConfig.DEBUG = false), no delay is added.
  </action>
  <acceptance_criteria>
    - `YoloScanFragment.kt` calls `setDetections()` on `PipelineState.Detected` when `BuildConfig.DEBUG`
    - `YoloScanFragment.kt` has a 3-second delayed clear after showing overlay (per D-16)
    - `YoloScanFragment.kt` logs each detection with `OnnxYoloEngine-Debug` tag
    - `VisionScanFragment.kt` logs detections in multi-mode when `BuildConfig.DEBUG`
    - Debug overlay is gated by `BuildConfig.DEBUG` (per D-17)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>Debug overlay renders YOLOE detections with 3-second pause in debug builds, Logcat shows detection details</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DI container → Engine | Hilt-injected dependency, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | Debug overlay | accept | Only renders in BuildConfig.DEBUG builds, stripped in release |
| T-08-05 | Denial of Service | 3-second delay | accept | Only in debug builds, production flow unaffected |
</threat_model>

<verification>
1. `./gradlew compileDebugKotlin` passes
2. InferenceModule provides OnnxYoloEngine
3. Debug overlay code gated by BuildConfig.DEBUG
4. Logcat tags present in both fragments
</verification>

<success_criteria>
- OnnxYoloEngine wired into DI as YoloDetector
- Debug overlay renders detection bounding boxes in YoloScanFragment
- Debug log messages show detection details
- Production builds have no overlay delay
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-yolo-hardening/08-09-SUMMARY.md`
</output>
