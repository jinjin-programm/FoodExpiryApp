---
phase: 08-yolo-hardening
plan: 03
type: execute
wave: 2
depends_on: ["08-04-PLAN"]
files_modified:
  - app/src/main/java/com/example/foodexpiryapp/presentation/ui/vision/VisionScanFragment.kt
  - app/src/main/res/layout/fragment_vision_scan.xml
  - app/src/main/java/com/example/foodexpiryapp/presentation/viewmodel/ChatViewModel.kt
autonomous: true
requirements: [YOLO-01]
user_setup: []

must_haves:
  truths:
    - "VisionScanFragment has a toggle button (ToggleButton/Chip) for switching scan mode"
    - "Two modes available: Single (direct LLM) and Multi (YOLO + LLM pipeline)"
    - "Default mode is Single, persisted in DataStore"
    - "When Multi mode selected, capture triggers DetectionPipeline instead of direct LLM call"
    - "When Multi mode results complete, navigates to ConfirmationFragment"
  artifacts:
    - path: "app/src/main/res/layout/fragment_vision_scan.xml"
      provides: "Updated layout with toggle button"
      contains: "toggleScanMode|chipSingle|chipMulti"
    - path: "app/src/main/java/com/example/foodexpiryapp/presentation/ui/vision/VisionScanFragment.kt"
      provides: "Mode switching logic + DetectionPipeline integration"
      contains: "startDetectionPipeline|scanMode"
  key_links:
    - from: "VisionScanFragment.kt"
      to: "DetectionPipeline"
      via: "ViewModel or direct call when multi-mode active"
      pattern: "detectAndClassify|DetectionPipeline"
---

<objective>
Add scan mode toggle to VisionScanFragment (AI Vision Scan) so users can choose between single-image analysis (direct LLM) and multi-object YOLO analysis (local YOLO detection + remote LLM classification).

Per D-06: Toggle button on VisionScanFragment for mode selection.
Per D-07: Default is single-image analysis, persisted in DataStore.
Per D-08: YOLO Scan tab (YoloScanFragment) stays unchanged.

Output: Users can toggle between single and multi-object scan modes on the AI Vision Scan screen.
</objective>

<execution_context>
@.planning/phases/08-yolo-hardening/08-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-yolo-hardening/08-CONTEXT.md
</context>

<tasks>

<task id="1" name="Add toggle UI to VisionScanFragment layout">
<read_first>
- app/src/main/res/layout/fragment_vision_scan.xml
</read_first>
<action>
Add a MaterialButtonToggleGroup to `fragment_vision_scan.xml`, positioned above or overlapping the camera preview at the top center:

```xml
<com.google.android.material.button.MaterialButtonToggleGroup
    android:id="@+id/toggleScanMode"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal|top"
    android:layout_marginTop="60dp"
    app:singleSelection="true"
    app:selectionRequired="true"
    android:background="@drawable/rounded_bg_semi_transparent"
    android:elevation="4dp">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSingleMode"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:text="Single"
        android:textSize="12sp"
        app:cornerRadius="18dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnMultiMode"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:text="Multi"
        android:textSize="12sp"
        app:cornerRadius="18dp" />

</com.google.android.material.button.MaterialButtonToggleGroup>
```

If `rounded_bg_semi_transparent` drawable doesn't exist, create it: a rounded rectangle with semi-transparent white/black background (depending on theme) suitable for overlaying on camera preview.

Position the toggle group so it doesn't overlap with the floating back button (top-left) or capture button (bottom-center). Top-center with margin is ideal.
</action>
<acceptance_criteria>
- `fragment_vision_scan.xml` contains `MaterialButtonToggleGroup` with id `toggleScanMode`
- Contains two child buttons: `btnSingleMode` and `btnMultiMode`
- Layout does not overlap with back button or capture button
- Build succeeds
</acceptance_criteria>
</task>

<task id="2" name="Add scan mode state and toggle logic in VisionScanFragment">
<read_first>
- app/src/main/java/com/example/foodexpiryapp/presentation/ui/vision/VisionScanFragment.kt
- app/src/main/java/com/example/foodexpiryapp/data/local/SettingsDataStore.kt (or equivalent DataStore pattern)
</read_first>
<action>
1. Add scan mode enum or boolean state to VisionScanFragment:
```kotlin
private var isMultiMode = false
```

2. In `onViewCreated` or `setupUI`, set up toggle listener:
```kotlin
binding.toggleScanMode.addOnButtonCheckedListener { _, checkedId, isChecked ->
    if (isChecked) {
        isMultiMode = (checkedId == R.id.btnMultiMode)
        // Persist preference
        lifecycleScope.launch {
            settingsDataStore.setScanMode(if (isMultiMode) "multi" else "single")
        }
    }
}

// Restore saved preference
lifecycleScope.launch {
    val savedMode = settingsDataStore.getScanMode()
    isMultiMode = savedMode == "multi"
    binding.toggleScanMode.check(if (isMultiMode) R.id.btnMultiMode else R.id.btnSingleMode)
}
```

3. Add `SettingsDataStore.setScanMode(mode: String)` and `SettingsDataStore.getScanMode(): Flow<String>` if they don't exist, using the existing DataStore pattern in the project.
</action>
<acceptance_criteria>
- `VisionScanFragment.kt` contains `toggleScanMode` reference from binding
- Toggle checked listener updates `isMultiMode` state
- Scan mode preference persisted and restored from DataStore
- Build succeeds
</acceptance_criteria>
</task>

<task id="3" name="Wire multi-mode to DetectionPipeline">
<read_first>
- app/src/main/java/com/example/foodexpiryapp/presentation/ui/vision/VisionScanFragment.kt
- app/src/main/java/com/example/foodexpiryapp/inference/pipeline/DetectionPipeline.kt
- app/src/main/java/com/example/foodexpiryapp/presentation/viewmodel/YoloScanViewModel.kt
</read_first>
<action>
Modify the capture handler in `VisionScanFragment` to branch based on scan mode:

1. Find where the current capture triggers LLM inference (likely in `captureAndDetect()` or similar method)
2. Add branching logic:

```kotlin
private fun captureAndDetect() {
    val bitmapCopy = latestBitmap?.let { Bitmap.createBitmap(it) } ?: return
    
    if (isMultiMode) {
        // Multi-object mode: use DetectionPipeline (YOLO + LLM)
        startMultiObjectDetection(bitmapCopy)
    } else {
        // Single mode: existing behavior — direct LLM call
        startSingleImageAnalysis(bitmapCopy)
    }
}
```

3. Implement `startMultiObjectDetection(bitmap: Bitmap)`:
   - Either inject DetectionPipeline directly (via Hilt) or create a simplified ViewModel
   - Collect PipelineState emissions
   - Show progress overlay (reuse existing progress pattern)
   - On `PipelineState.Complete`: navigate to ConfirmationFragment with sessionId
   - On `PipelineState.Error`: show error toast
   - On `PipelineState.NoDetection`: show "No items detected" message

4. Keep existing single-mode logic unchanged — it already works via FoodVisionClient.

The simplest approach is to inject `DetectionPipeline` into VisionScanFragment via Hilt (`@AndroidEntryPoint` already exists) and collect its Flow output in `lifecycleScope`.
</action>
<acceptance_criteria>
- `VisionScanFragment.kt` branches capture logic based on `isMultiMode`
- Multi-mode creates bitmap copy and starts DetectionPipeline
- Multi-mode collects PipelineState and navigates to ConfirmationFragment on completion
- Single-mode uses existing FoodVisionClient flow unchanged
- Build succeeds
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` exits 0
2. `fragment_vision_scan.xml` contains MaterialButtonToggleGroup
3. `VisionScanFragment.kt` reads toggle state and branches capture logic
4. Multi-mode triggers DetectionPipeline
5. Single-mode uses existing FoodVisionClient
</verification>
