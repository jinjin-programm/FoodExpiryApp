---
phase: 08-yolo-hardening
plan: 03
type: execute
wave: 3
depends_on: ["08-08-PLAN", "08-09-PLAN"]
files_modified:
  - app/src/test/java/com/example/foodexpiryapp/inference/yolo/YoloEPostprocessorTest.kt
autonomous: false
requirements: [YOLO-01, YOLO-03]

user_setup:
  - service: Android device testing
    why: "YOLOE ONNX inference requires real hardware — emulator lacks camera + ONNX Runtime behavior may differ"
    steps:
      - "Build APK: ./gradlew assembleDebug"
      - "Install: adb install -r app/build/outputs/apk/debug/app-debug.apk"
      - "Launch: adb shell am start -n com.example.foodexpiryapp/.presentation.MainActivity"
      - "Navigate to YOLO Scan tab"
      - "Capture a photo of food items (fridge shelf, fruit bowl, etc.)"
      - "Watch Logcat: adb logcat -s OnnxYoloEngine OnnxYoloEngine-Debug DetectionPipeline"
      - "Expected: detections logged with label, confidence, bbox"
      - "If debug overlay enabled: bounding boxes visible for 3 seconds"
      - "Expected: LLM classification proceeds after YOLO detection"
      - "Try multi-mode (VisionScanFragment toggle): same detection + classification flow"

must_haves:
  truths:
    - "parseYoloEDetections unit tests pass for valid, empty, and edge-case inputs"
    - "User has tested on real device and confirmed YOLOE produces Logcat output"
    - "Build succeeds and APK installs on device"
    - "Debug overlay shows bounding boxes in debug builds"
    - "YOLOE detections flow through DetectionPipeline to LLM classification"
  artifacts:
    - path: "app/src/test/java/com/example/foodexpiryapp/inference/yolo/YoloEPostprocessorTest.kt"
      provides: "Unit tests for YOLOE output parsing"
      min_lines: 60
  key_links:
    - from: "YoloEPostprocessorTest.kt"
      to: "MnnYoloPostprocessor.kt"
      via: "tests parseYoloEDetections()"
      pattern: "parseYoloEDetections"
---

<objective>
Validate YOLOE detection pipeline with unit tests and real device verification.

Purpose: Per D-25: Test on real device to confirm YOLOE detects objects. Per D-26: Unit tests for post-processing pure functions. This is the final plan that proves the entire ONNX Runtime + YOLOE pipeline works end-to-end.

Output: Unit tests passing, real device verification confirmed by user.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-yolo-hardening/08-CONTEXT.md
@.planning/phases/08-yolo-hardening/08-08-SUMMARY.md
@.planning/phases/08-yolo-hardening/08-09-SUMMARY.md

<interfaces>
From app/src/main/java/com/example/foodexpiryapp/inference/yolo/MnnYoloPostprocessor.kt:
```kotlin
object MnnYoloPostprocessor {
    fun parseYoloEDetections(
        outputArray: FloatArray,
        numDetections: Int,
        numValues: Int,
        originalWidth: Int,
        originalHeight: Int,
        confidenceThreshold: Float = 0.5f,
        maxDetections: Int = 8
    ): List<DetectionResult>

    fun applyNms(detections: List<DetectionResult>, iouThreshold: Float): List<DetectionResult>
    fun cropDetection(sourceBitmap: Bitmap, detection: DetectionResult): Bitmap?
}
```

DetectionResult model (normalized 0f-1f bbox):
```kotlin
data class DetectionResult(
    val id: Int = 0,
    val boundingBox: RectF,
    val cropBitmap: Bitmap? = null,
    val label: String,
    val category: FoodCategory,
    val confidence: Float,
    val foodIdentification: FoodIdentification? = null,
    val status: DetectionStatus = DetectionStatus.PENDING
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for YOLOE post-processing</name>
  <files>app/src/test/java/com/example/foodexpiryapp/inference/yolo/YoloEPostprocessorTest.kt</files>
  <read_first>
    - app/src/main/java/com/example/foodexpiryapp/inference/yolo/MnnYoloPostprocessor.kt (functions under test)
    - app/src/main/java/com/example/foodexpiryapp/domain/model/DetectionResult.kt (model)
  </read_first>
  <action>
Per D-26: Unit tests for post-processing pure functions.

Create `app/src/test/java/com/example/foodexpiryapp/inference/yolo/YoloEPostprocessorTest.kt`:

Test cases for `parseYoloEDetections()`:
1. **testParseYoloE_validDetections**: Create a FloatArray simulating (3, 38) output with 3 detections. Set bbox at indices 0-3 (e.g., [100, 100, 300, 300] in 640 space), confidence at index 36 (>0.5), class_id at index 37. Original image 1920×1080. Assert 3 DetectionResults returned with normalized bbox, correct label ("class_X"), and confidence matching input.

2. **testParseYoloE_filtersLowConfidence**: Create FloatArray with 5 detections, 3 above threshold (0.7, 0.8, 0.6) and 2 below (0.2, 0.3). Assert only 3 results returned.

3. **testParseYoloE_maxDetections**: Create FloatArray with 10 detections all above threshold. Set maxDetections=8. Assert exactly 8 results, and they are the top-8 by confidence.

4. **testParseYoloE_emptyOutput**: Empty FloatArray (0 detections). Assert empty list returned.

5. **testParseYoloE_degenerateBoxes**: Detection where x2<=x1 or y2<=y1. Assert degenerate box is skipped.

6. **testParseYoloE_normalizedCoordinates**: Verify bbox is normalized to 0f-1f range regardless of original image size. Input bbox [0, 0, 640, 640] with 640x640 original should give [0, 0, 1, 1].

Test cases for `applyNms()` (existing, but include for regression):
7. **testApplyNms_removesOverlapping**: Two boxes with IoU > 0.45. Assert only higher-confidence box kept.

8. **testApplyNms_keepsNonOverlapping**: Two boxes with IoU < 0.45. Assert both kept.

Test cases for `cropDetection()` (existing, regression):
9. **testCropDetection_validCrop**: Create 640×480 bitmap, detection with bbox [0.1, 0.2, 0.5, 0.8]. Assert cropped bitmap has correct dimensions.

10. **testCropDetection_outOfBounds**: Detection with bbox [0.9, 0.9, 1.5, 1.5]. Assert returns null.

Helper: Create a function `createYoloEOutput(numDetections: Int, configDetections: List<Triple<FloatArray, Float, Int>>): FloatArray` to easily construct test data matching the (N, 38) format.
  </action>
  <acceptance_criteria>
    - `YoloEPostprocessorTest.kt` exists with at least 10 test functions
    - Tests cover: valid detections, confidence filtering, max cap, empty input, degenerate boxes, normalized coords, NMS overlapping, NMS non-overlapping, valid crop, out-of-bounds crop
    - All tests call `parseYoloEDetections()` with FloatArray matching (N, 38) format
    - Confidence at index numValues-2 (36), class_id at index numValues-1 (37)
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.example.foodexpiryapp.inference.yolo.YoloEPostprocessorTest" 2>&1 | tail -10</automated>
  </verify>
  <done>All unit tests pass for parseYoloEDetections, applyNms, and cropDetection</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real device verification of YOLOE detection pipeline</name>
  <files>None — human testing only</files>
  <action>Build APK, install on device, test Single-mode and Multi-mode AI Vision Scan, verify YOLOE detections appear in Logcat and debug overlay, confirm LLM classification follows.</action>
  <what_built>Complete YOLOE ONNX detection pipeline — OnnxYoloEngine + DI wiring + debug overlay + unit tests</what_built>
  <how-to-verify>
    **Step 1: Build and Install**
    ```bash
    ./gradlew assembleDebug
    adb install -r app/build/outputs/apk/debug/app-debug.apk
    ```

    **Step 2: Open Logcat**
    ```bash
    adb logcat -s OnnxYoloEngine OnnxYoloEngine-Debug DetectionPipeline
    ```

    **Step 3: Single-mode Test (AI Vision Scan)**
    1. Open app → Navigate to "AI Vision Scan"
    2. Ensure toggle is on "Single" mode
    3. Point camera at a food item and capture
    4. Expected: LLM classifies the food item (existing behavior, should still work)

    **Step 4: Multi-mode Test (AI Vision Scan)**
    1. Toggle to "Multi" mode
    2. Point camera at multiple food items (fridge shelf, fruit bowl) and capture
    3. Expected Logcat output:
       - `OnnxYoloEngine: Session created with inputs=[images], outputs=[output0, output1]`
       - `OnnxYoloEngine-Debug: Detection N: label=class_X, conf=0.YY, bbox=[x1, y1, x2, y2]`
       - `DetectionPipeline: Stage 1 complete — N detections`
       - `DetectionPipeline: Classifying 1/N...`
    4. Expected: Debug overlay shows bounding boxes for 3 seconds (debug build only)
    5. Expected: LLM classification follows, results shown in ConfirmationFragment

    **Step 5: YOLO Scan Tab Test**
    1. Navigate to "YOLO Scan" tab
    2. Capture a photo with food items
    3. Expected: Same Logcat output + overlay rendering
    4. Expected: Classification results appear in confirmation list

    **What to report:**
    - Number of detections per scan (should be 1-8)
    - Whether bounding boxes appear at correct positions
    - Whether LLM classification succeeds after detection
    - Any crashes or errors in Logcat
  </how-to-verify>
  <resume-signal>Type "approved" if detections work correctly, or describe issues encountered</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Production code | Test code exercises pure functions, no trust boundary concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Elevation of Privilege | Test execution | accept | Tests run in JVM sandbox, no privilege escalation possible |
</threat_model>

<verification>
1. `./gradlew test --tests "*.YoloEPostprocessorTest"` passes (all 10+ tests)
2. `./gradlew assembleDebug` produces APK
3. Human confirms detection works on real device
</verification>

<success_criteria>
- All unit tests pass (parseYoloEDetections, applyNms, cropDetection)
- User has tested on device and confirmed:
  - YOLOE detects objects in camera frames
  - Logcat shows detection results
  - LLM classification follows detection
  - No crashes during the flow
</success_criteria>

<output>
After completion, create `.planning/phases/08-yolo-hardening/08-10-SUMMARY.md`
</output>
