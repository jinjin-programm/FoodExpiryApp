---
phase: 08-yolo-hardening
plan: 02
type: execute
wave: 1
depends_on: ["08-04-PLAN"]
files_modified:
  - app/src/main/java/com/example/foodexpiryapp/inference/pipeline/DetectionPipeline.kt
  - app/src/main/java/com/example/foodexpiryapp/presentation/viewmodel/YoloScanViewModel.kt
  - app/src/main/java/com/example/foodexpiryapp/presentation/viewmodel/ConfirmationViewModel.kt
  - app/src/main/java/com/example/foodexpiryapp/presentation/adapter/DetectionResultAdapter.kt
  - app/src/main/res/values/colors.xml
autonomous: true
requirements: [YOLO-01, YOLO-03]
user_setup: []

must_haves:
  truths:
    - "DetectionPipeline depends on YoloDetector interface instead of MnnYoloEngine concrete class"
    - "DetectionPipeline still uses FoodVisionClient (Ollama/LM Studio) for LLM classification"
    - "Bitmap double-recycle in DetectionPipeline fixed — cropBitmap nulled after recycle"
    - "Defensive bitmap copy in YoloScanFragment captureAndDetect()"
    - "ConfirmationViewModel.saveAll() reports errors via StateFlow"
    - "DetectionResultAdapter uses colors.xml resources instead of Color.parseColor()"
    - "cancelDetection() propagates cancellation through isActive check"
  artifacts:
    - path: "app/src/main/java/com/example/foodexpiryapp/inference/pipeline/DetectionPipeline.kt"
      provides: "Refactored pipeline using YoloDetector interface + bitmap safety fixes"
      contains: "YoloDetector"
    - path: "app/src/main/java/com/example/foodexpiryapp/presentation/adapter/DetectionResultAdapter.kt"
      provides: "Colors extracted to colors.xml"
      contains: "ContextCompat.getColor"
  key_links:
    - from: "DetectionPipeline.kt"
      to: "YoloDetector"
      via: "constructor injection"
      pattern: "private val yoloDetector: YoloDetector"
---

<objective>
Refactor DetectionPipeline to use the new YoloDetector interface (TFLite), fix all code quality issues (bitmap safety, error reporting, color extraction, cancel propagation). This is the core wiring plan.

Per D-09: Replace MnnYoloEngine dependency with YoloDetector interface.
Per D-13: Fix bitmap double-recycle.
Per D-14: Add defensive bitmap copy.
Per D-15: Fix ConfirmationViewModel silent error swallowing.
Per D-16: Extract hardcoded colors.
Per D-17: Verify cancel propagation.

Output: Clean pipeline + code quality fixes, all building successfully.
</objective>

<execution_context>
@.planning/phases/08-yolo-hardening/08-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-yolo-hardening/08-CONTEXT.md
</context>

<tasks>

<task id="1" name="Refactor DetectionPipeline to use YoloDetector">
<read_first>
- app/src/main/java/com/example/foodexpiryapp/inference/pipeline/DetectionPipeline.kt
- app/src/main/java/com/example/foodexpiryapp/inference/tflite/YoloDetector.kt
</read_first>
<action>
Update `DetectionPipeline.kt`:

1. Replace `import com.example.foodexpiryapp.inference.yolo.MnnYoloEngine` with `import com.example.foodexpiryapp.inference.tflite.YoloDetector`
2. Replace constructor parameter `private val yoloEngine: MnnYoloEngine` with `private val yoloDetector: YoloDetector`
3. Replace ALL references to `yoloEngine` → `yoloDetector`:
   - `yoloEngine.isModelLoaded()` → `yoloDetector.isModelLoaded()`
   - `yoloEngine.loadModel()` → `yoloDetector.loadModel()`
   - `yoloEngine.detect(bitmap)` → `yoloDetector.detect(bitmap)`
   - `yoloEngine.unloadModel()` → `yoloDetector.unloadModel()`
4. Keep all other imports (FoodVisionClient, PipelineState, etc.) unchanged
5. Keep MnnYoloPostprocessor.cropDetection call — this pure function is still used for cropping
</action>
<acceptance_criteria>
- `DetectionPipeline.kt` does NOT contain `MnnYoloEngine` anywhere
- `DetectionPipeline.kt` contains `import com.example.foodexpiryapp.inference.tflite.YoloDetector`
- `DetectionPipeline.kt` constructor contains `private val yoloDetector: YoloDetector`
- All `yoloEngine` references replaced with `yoloDetector`
- `FoodVisionClient`, `OllamaVisionClient`, `LmStudioVisionClient` imports remain
- `MnnYoloPostprocessor.cropDetection` call still present
- Build succeeds
</acceptance_criteria>
</task>

<task id="2" name="Fix bitmap double-recycle in DetectionPipeline">
<read_first>
- app/src/main/java/com/example/foodexpiryapp/inference/pipeline/DetectionPipeline.kt (current line ~136)
</read_first>
<action>
In `DetectionPipeline.detectAndClassify()`, find the bitmap recycle section (currently around line 136):

BEFORE:
```kotlin
// Recycle crop bitmap IMMEDIATELY per YOLO-08/PITFALL-6
detection.cropBitmap?.recycle()
results.add(result.copy(cropBitmap = null))
```

AFTER:
```kotlin
// Per D-13: Recycle crop bitmap and null out reference to prevent stale access
detection.cropBitmap?.recycle()
detection.cropBitmap?.let { 
    results.add(result.copy(cropBitmap = null))
}
```

Ensure `result.copy(cropBitmap = null)` is ALWAYS called (not just when bitmap exists). The result added to the list must always have cropBitmap = null to prevent any downstream stale bitmap access.
</action>
<acceptance_criteria>
- `DetectionPipeline.kt` bitmap recycle section calls `recycle()` then adds `result.copy(cropBitmap = null)` to results
- No code path where result with non-null cropBitmap is added to final results list
- Build succeeds
</acceptance_criteria>
</task>

<task id="3" name="Fix ConfirmationViewModel saveAll error reporting">
<read_first>
- app/src/main/java/com/example/foodexpiryapp/presentation/viewmodel/ConfirmationViewModel.kt
</read_first>
<action>
Find the `saveAll()` method. Locate the empty catch block (around line 104).

BEFORE:
```kotlin
} catch (e: Exception) {
    // empty catch — silent error swallowing
}
```

AFTER:
```kotlin
} catch (e: Exception) {
    Log.e(TAG, "Error saving detection results", e)
    _saveResult.value = SaveResult.Error(e.message ?: "Failed to save items")
}
```

If `_saveResult` StateFlow doesn't exist, create it:
- Add `private val _saveResult = MutableStateFlow<SaveResult>(SaveResult.Idle)`
- Add `val saveResult: StateFlow<SaveResult> = _saveResult.asStateFlow()`
- Add sealed class `SaveResult` with `Idle`, `Success(count: Int)`, `Error(message: String)` subtypes
- In the success path, add `_saveResult.value = SaveResult.Success(savedCount)`
</action>
<acceptance_criteria>
- `ConfirmationViewModel.kt` catch block in `saveAll()` contains `Log.e` call
- `ConfirmationViewModel.kt` catch block emits error state (not empty)
- Build succeeds
</acceptance_criteria>
</task>

<task id="4" name="Extract hardcoded colors in DetectionResultAdapter">
<read_first>
- app/src/main/java/com/example/foodexpiryapp/presentation/adapter/DetectionResultAdapter.kt
- app/src/main/res/values/colors.xml
</read_first>
<action>
1. Find all `Color.parseColor(...)` calls in `DetectionResultAdapter.kt` (should be ~6 occurrences)
2. For each color, add a named resource to `colors.xml`:
   - e.g., `Color.parseColor("#4CAF50")` → `<color name="detection_status_classified">#4CAF50</color>`
   - e.g., `Color.parseColor("#F44336")` → `<color name="detection_status_failed">#F44336</color>`
   - e.g., `Color.parseColor("#FF9800")` → `<color name="detection_status_pending">#FF9800</color>`
   - Map all 6 colors with descriptive names following `detection_*` naming convention
3. Replace each `Color.parseColor("...")` with `ContextCompat.getColor(context, R.color.detection_xxx)`
4. Ensure `context` is available (from `itemView.context` or passed via constructor)
</action>
<acceptance_criteria>
- `DetectionResultAdapter.kt` contains ZERO occurrences of `Color.parseColor`
- `DetectionResultAdapter.kt` contains `ContextCompat.getColor` for each previously hardcoded color
- `colors.xml` contains at least 4 new `<color>` entries with `detection_` prefix
- Build succeeds
</acceptance_criteria>
</task>

<task id="5" name="Add defensive bitmap copy in YoloScanFragment">
<read_first>
- app/src/main/java/com/example/foodexpiryapp/presentation/ui/yolo/YoloScanFragment.kt
</read_first>
<action>
Find `captureAndDetect()` method. Find where `latestBitmap` is passed to the pipeline.

BEFORE (or similar):
```kotlin
viewModel.startDetection(latestBitmap!!)
```

AFTER:
```kotlin
// Per D-14: Defensive copy — camera frame callback can overwrite latestBitmap during inference
val bitmapCopy = latestBitmap?.let { Bitmap.createBitmap(it) }
if (bitmapCopy != null) {
    viewModel.startDetection(bitmapCopy)
} else {
    isCapturing = false
    Toast.makeText(context, "Failed to capture image", Toast.LENGTH_SHORT).show()
}
```

Also apply the same pattern to `handleGalleryImage` if it calls `viewModel.startDetection(bitmap)` directly.
</action>
<acceptance_criteria>
- `YoloScanFragment.kt` `captureAndDetect()` creates a bitmap copy via `Bitmap.createBitmap(it)` before passing to viewModel
- No direct passing of `latestBitmap` to `viewModel.startDetection`
- Build succeeds
</acceptance_criteria>
</task>

<task id="6" name="Verify cancel propagation">
<read_first>
- app/src/main/java/com/example/foodexpiryapp/inference/pipeline/DetectionPipeline.kt (isActive check)
- app/src/main/java/com/example/foodexpiryapp/presentation/viewmodel/YoloScanViewModel.kt (cancelDetection)
</read_first>
<action>
Verify the cancellation chain works:

1. In `DetectionPipeline.detectAndClassify()`, confirm the `isActive` check exists in the classification loop (it should already be there from prior implementation around line 109)
2. In `YoloScanViewModel.cancelDetection()`, confirm it calls `pipelineJob?.cancel()` (already there) AND `yoloDetectionRepository.cancelDetection()` (already there)
3. If any gap found, add the missing piece. The expected chain is:
   - User taps cancel → `viewModel.cancelDetection()` → `pipelineJob?.cancel()` → coroutine cancellation → `isActive` becomes false in pipeline loop → `PipelineState.Cancelled` emitted
4. Confirm `PipelineState.Cancelled` is handled in `YoloScanViewModel` uiState mapping (already there: `is PipelineState.Cancelled -> YoloScanUiState.Ready`)
</action>
<acceptance_criteria>
- `DetectionPipeline.kt` classification loop contains `if (!isActive)` check
- `YoloScanViewModel.kt` `cancelDetection()` calls `pipelineJob?.cancel()`
- `YoloScanViewModel.kt` handles `PipelineState.Cancelled` in state mapping
- Build succeeds
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` exits 0
2. `DetectionPipeline.kt` uses `YoloDetector` interface, not `MnnYoloEngine`
3. `DetectionResultAdapter.kt` has zero `Color.parseColor` calls
4. `ConfirmationViewModel.kt` catch block is not empty
5. `YoloScanFragment.kt` creates bitmap copy before pipeline call
</verification>
