---
phase: 03-polish-trust
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - app/src/main/res/layout/fragment_profile_settings.xml,
  - app/src/main/java/com/example/foodexpiryapp/presentation/ui/profile/ProfileSettingsFragment.kt
autonomous: true
requirements: [PROF-01]

must_haves:
  truths:
    - "Notification settings section appears at the TOP of the Profile Settings tab (before Personal Information)"
    - "Notification toggle, days-before slider, and reminder time are immediately visible without scrolling"
    - "Existing notification functionality (toggle, slider, time picker, save) still works correctly after reordering"
  artifacts:
    - path: "app/src/main/res/layout/fragment_profile_settings.xml"
      provides: "Reordered settings with notifications first"
      contains: "switchNotifications"
  key_links:
    - from: "ProfileSettingsFragment"
      to: "fragment_profile_settings.xml"
      via: "ViewBinding IDs"
      pattern: "switchNotifications|sliderDaysBefore|btnSetTime"
    - from: "ProfileSettingsFragment"
      to: "ProfileViewModel"
      via: "existing viewModel methods"
      pattern: "updateNotificationsEnabled|updateDefaultDaysBefore|updateNotificationTime"
---

<objective>
Move notification settings to the top of the Profile Settings tab so food protection controls are immediately visible.

Purpose: Emphasize that notifications are the primary food protection tool — users should see expiry alert settings first, before personal info or dietary preferences.
Output: Reordered Settings tab with Notification Settings as the first section.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-trust/03-02-PLAN.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From presentation/ui/profile/ProfileSettingsFragment.kt:
- Section order in current layout: Personal Info → Household → Notifications → Dietary → Save/Discard
- After Plan 02: Personal Info → Household → Notifications → Dietary → Privacy → Save/Discard
- Fragment uses FragmentProfileSettingsBinding
- All notification IDs: switchNotifications, sliderDaysBefore, textDaysBeforeLabel, textNotificationTimeLabel, btnSetTime
- ViewModel methods unchanged: updateNotificationsEnabled(), updateDefaultDaysBefore(), updateNotificationTime()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder Profile Settings to place Notification Settings at the top</name>
  <files>
    app/src/main/res/layout/fragment_profile_settings.xml
  </files>
  <action>
    Reorder the sections in `fragment_profile_settings.xml` so that Notification Settings appears FIRST (after the initial padding, before Personal Information).

    Current order after Plan 02:
    1. Personal Information (photo + name)
    2. Household &amp; Servings (slider)
    3. Notification Settings (switch + slider + time picker)
    4. Dietary Preferences (chips)
    5. Privacy &amp; Data (card)
    6. Save/Discard buttons

    New order:
    1. Notification Settings (switch + slider + time picker) — MOVE TO TOP
    2. Personal Information (photo + name)
    3. Household &amp; Servings (slider)
    4. Dietary Preferences (chips)
    5. Privacy &amp; Data (card)
    6. Save/Discard buttons

    Steps:
    1. Read the current `fragment_profile_settings.xml` (which now includes the Privacy section from Plan 02)
    2. Cut the entire Notification Settings section block (from the section header LinearLayout through the "Change Time" button and the divider after it)
    3. Paste it as the FIRST section after the root LinearLayout's opening tag and padding
    4. Adjust margins: the notification section should have `android:layout_marginTop="16dp"` (instead of the current larger margin since it's now the first section)
    5. The section that follows (Personal Information) should have `android:layout_marginTop="24dp"` as a separator

    CRITICAL: Do NOT change any IDs, ViewBinding references, or functional code. This is purely a layout reordering. All existing FragmentProfileSettingsBinding bindings in ProfileSettingsFragment.kt must continue to work unchanged because IDs remain the same.
  </action>
  <verify>
    <automated>./gradlew assembleDebug 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Notification Settings section is the first visible section in the Profile Settings tab. All notification controls (toggle, slider, time picker) appear before personal info. No IDs changed, no Fragment code changes needed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Notification Settings moved to top of Profile Settings tab (PROF-01)</what-built>
  <how-to-verify>
    1. Build and launch the app: `./gradlew installDebug`
    2. Navigate to Profile tab → Settings sub-tab
    3. Verify: Notification Settings section is the FIRST visible section (no scrolling needed to see the toggle, slider, and time)
    4. Toggle "Enable Expiry Notifications" off and on — verify it works
    5. Adjust the "Notify days before expiry" slider — verify the label updates
    6. Tap "Change Time" — verify the time picker dialog appears
    7. Scroll down — verify all other sections are still present in order: Personal Info, Household, Dietary, Privacy, Save/Discard
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layout reorder → Runtime | No new trust boundaries; same IDs, same bindings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | T | Layout reorder | accept | Pure XML reordering, no code logic changes, no security impact |
</threat_model>

<verification>
1. `./gradlew assembleDebug` compiles without errors
2. Notification Settings section appears first in Settings tab
3. All notification controls (toggle, slider, time picker) work correctly
4. All other sections remain functional in their new order
5. No ViewBinding compilation errors (all IDs preserved)
</verification>

<success_criteria>
1. Notification settings are prominently accessible near the top of the Profile Settings module (PROF-01)
2. No existing notification functionality is broken by the reorder
3. All sections remain accessible by scrolling
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-trust/03-03-SUMMARY.md`
</output>
