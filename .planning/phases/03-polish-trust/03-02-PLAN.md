---
phase: 03-polish-trust
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/example/foodexpiryapp/presentation/ui/profile/ProfileSettingsFragment.kt
  - app/src/main/res/layout/fragment_profile_settings.xml
  - app/src/main/java/com/example/foodexpiryapp/presentation/ui/profile/ProfileAccountFragment.kt
  - app/src/main/res/layout/fragment_profile_account.xml
autonomous: true
requirements: [PROF-02, PROF-03]

must_haves:
  truths:
    - "User sees clear privacy copy explaining local data storage, AI usage (on-device ML Kit, not cloud), and no data selling"
    - "User can access privacy information without signing in with Google"
    - "Google Sign-In is presented as optional with clear 'Skip' or no-sign-in path"
    - "Profile is fully usable without any Google account linked"
  artifacts:
    - path: "app/src/main/res/layout/fragment_profile_settings.xml"
      provides: "Updated settings with privacy section"
      contains: "privacyCard"
    - path: "app/src/main/res/layout/fragment_profile_account.xml"
      provides: "Updated account with optional Google sign-in"
      contains: "btnGoogleSignIn"
  key_links:
    - from: "ProfileSettingsFragment"
      to: "fragment_profile_settings.xml"
      via: "ViewBinding inflation"
      pattern: "FragmentProfileSettingsBinding"
    - from: "ProfileAccountFragment"
      to: "fragment_profile_account.xml"
      via: "ViewBinding inflation"
      pattern: "FragmentProfileAccountBinding"
---

<objective>
Add privacy and data usage explanations to the Profile, and ensure Google Sign-In is clearly optional with a skip path.

Purpose: Build user trust through transparency — explain how data is stored locally, how AI works on-device, and make Google login clearly optional.
Output: Privacy section in Settings tab, optional Google Sign-In with clear skip option.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From presentation/ui/profile/ProfileSettingsFragment.kt:
- Uses FragmentProfileSettingsBinding, @AndroidEntryPoint
- Has sections: Personal Information (photo + name), Household &amp; Servings (slider), Notification Settings (switch + slider + time picker), Dietary Preferences (chips)
- Save/Discard buttons at bottom

From presentation/ui/profile/ProfileAccountFragment.kt:
- Uses FragmentProfileAccountBinding, @AndroidEntryPoint
- Google Sign-In button + Sign Out button
- Shows user name/email/photo when signed in
- Uses deprecated onActivityResult for GoogleSignIn

From presentation/ui/profile/ProfileTabsPagerAdapter.kt:
- 4 tabs: Settings (0), Account (1), Feedback (2), Help (3)
- Help tab currently shows "Coming soon..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add privacy and data usage section to Profile Settings tab</name>
  <files>
    app/src/main/res/layout/fragment_profile_settings.xml,
    app/src/main/java/com/example/foodexpiryapp/presentation/ui/profile/ProfileSettingsFragment.kt
  </files>
  <action>
    Add a privacy and data usage section at the BOTTOM of the Settings tab (after Dietary Preferences, before Save/Discard buttons).

    1. Update `fragment_profile_settings.xml` — Insert before the Save/Discard LinearLayout:

    ```xml
    <!-- Privacy & Data Section -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#DDDDDD"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="24dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="16dp"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="8dp"
            android:contentDescription="Privacy"
            app:tint="?attr/colorPrimary"
            app:srcCompat="@android:drawable/ic_lock_lock" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Privacy &amp; Data"
            android:textAppearance="?attr/textAppearanceSubtitle1"
            android:textColor="?attr/colorPrimary" />
    </LinearLayout>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/privacyCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/surface_container_lowest"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="How we handle your data"
                android:textAppearance="?attr/textAppearanceSubtitle2"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="• All food items and photos are stored locally on your device only."
                android:textAppearance="?attr/textAppearanceBody2" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="• Photo scanning uses on-device AI (Google ML Kit). Your images never leave your phone."
                android:textAppearance="?attr/textAppearanceBody2" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="• Recipe suggestions come from your inventory data — no personal data is shared with third parties."
                android:textAppearance="?attr/textAppearanceBody2" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="• Google Sign-In is optional. You can use all features without an account."
                android:textAppearance="?attr/textAppearanceBody2" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="• You can delete all your data anytime from the Account tab."
                android:textAppearance="?attr/textAppearanceBody2" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
    ```

    Insert this section between the ChipGroup and the Save/Discard LinearLayout (around line 224, before the spacing LinearLayout).

    2. In `ProfileSettingsFragment.kt` — No code changes needed for this static section. The privacy card is informational only. The fragment already inflates `fragment_profile_settings.xml` correctly.

    Follow existing layout patterns: use existing color references, MaterialCardView styling, and section headers matching the current design language.
  </action>
  <verify>
    <automated>./gradlew assembleDebug 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Privacy and data usage card is visible in the Settings tab. It explains local storage, on-device AI, optional Google login, and data deletion. Uses existing design language.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make Google Sign-In clearly optional with skip-friendly UX in Account tab</name>
  <files>
    app/src/main/res/layout/fragment_profile_account.xml,
    app/src/main/java/com/example/foodexpiryapp/presentation/ui/profile/ProfileAccountFragment.kt
  </files>
  <action>
    Update the Account tab to make Google Sign-In clearly optional and add a "local account" state.

    1. Read the current `fragment_profile_account.xml` first to see the existing layout.

    2. Update `fragment_profile_account.xml` — Restructure to show:
    - When NOT signed in: Show a welcome card explaining "Using FoodExpiryApp locally" with the text "You're using the app with a local account. All your data stays on this device. Sign in with Google to sync across devices (optional)."
    - The Google Sign-In button should have subtitle text "Optional — sync across devices" underneath it
    - Add a prominent "Continue without account" or visual indicator that the app works fully without signing in
    - When signed in: Keep existing behavior (show user info, sign out button)

    The layout should follow the existing design: MaterialCardView containers, surface_container_lowest backgrounds, 24dp corner radius, primary/tertiary colors.

    3. Update `ProfileAccountFragment.kt`:
    - Add visibility toggles for the "local account" welcome section vs signed-in section
    - When `state.isGoogleSignIn == false`: show the local account card, hide user info
    - When `state.isGoogleSignIn == true`: hide the local account card, show user info (existing behavior)
    - No new ViewModel methods needed — existing `uiState.isGoogleSignIn` drives the UI

    Key text content for the local account card:
    - Title: "Local Account"
    - Body: "All your food items, recipes, and preferences are stored on this device. No account needed."
    - Below Google Sign-In button: "Optional — sync across devices" (small, tertiary text)

    Do NOT remove any existing functionality. Only ADD the local account state and make Google sign-in visually secondary.
  </action>
  <verify>
    <automated>./gradlew assembleDebug 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    Account tab shows a "Local Account" welcome state when not signed in. Google Sign-In button has "Optional" subtitle. Profile is fully usable without Google account. Existing sign-in/sign-out flow still works.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Privacy section in Settings tab and optional Google Sign-In in Account tab (PROF-02, PROF-03)</what-built>
  <how-to-verify>
    1. Build and launch the app: `./gradlew installDebug`
    2. Navigate to Profile tab, then "Settings" sub-tab
    3. Scroll to bottom — verify the "Privacy &amp; Data" card is visible with 5 bullet points explaining local storage, on-device AI, optional login, and data deletion
    4. Switch to "Account" sub-tab (ensure NOT signed in with Google)
    5. Verify: A "Local Account" welcome card is shown explaining local data storage
    6. Verify: Google Sign-In button has "Optional" subtitle text
    7. Verify: The profile is fully functional without signing in — navigate to other tabs and back, everything works
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile UI → User | Static informational text, no data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | I | Privacy text content | accept | Static strings in XML layout, no user input, no injection risk |
| T-03-05 | S | Google Sign-In optional messaging | accept | Existing Google Sign-In flow unchanged, only UI text added |
</threat_model>

<verification>
1. `./gradlew assembleDebug` compiles without errors
2. Settings tab shows Privacy &amp; Data section at bottom
3. Privacy text mentions: local storage, on-device AI (ML Kit), optional Google login, data deletion
4. Account tab shows "Local Account" state when not signed in
5. Google Sign-In button has "Optional" subtitle
6. Profile fully functional without Google account
</verification>

<success_criteria>
1. Privacy copy explains data usage, AI behavior, and keeps Google Login optional (PROF-02)
2. Google Sign-In is clearly optional with a skip-friendly UX (PROF-03)
3. No existing functionality is broken
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-trust/03-02-SUMMARY.md`
</output>
